fix: add jsdom dev dependency for CalloutManager tests

The CI was failing because callout-manager.test.js imports jsdom
but it was not listed in package.json devDependencies.

Files changed:
- package.json — added jsdom to devDependencies
- package-lock.json — lockfile update
