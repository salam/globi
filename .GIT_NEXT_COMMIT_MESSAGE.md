# feat: add ThreeGlobeRenderer with full render pipeline

Summary:

- Created ThreeGlobeRenderer integrating EarthBuilder, GraticuleBuilder, MarkerManager, ArcPathManager, RegionManager, and CalloutManager.
- Globe uses fixed camera at (0,0,3/zoom); globe group rotates for pan/zoom.
- rAF render loop with dirty-flag optimization.
- init() creates WebGLRenderer + CSS2DRenderer, scene, camera, lights, globe hierarchy.
- renderScene() updates all sub-managers and CSS2D callout labels with hover/click wiring.
- Texture loading with SRGBColorSpace and textureError event on failure.
- hitTest() uses Raycaster for markers → arcs → regions.
- projectPointToClient() applies globe quaternion before NDC projection.
- calloutManager.updateVisibility() receives globe quaternion each frame.
- destroy() stops loop, disposes all GPU resources and DOM elements.
- Created 6 unit tests for public API shape, camera state, panBy, zoomBy, flyTo, destroy.
- All 125 tests pass.

Diff (paths):

- src/renderer/threeGlobeRenderer.js (new)
- tests/three-renderer.test.js (new)
- RELEASE_NOTES.md
- .GIT_NEXT_COMMIT_MESSAGE.md
