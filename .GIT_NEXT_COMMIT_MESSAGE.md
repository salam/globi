# feat: add CalloutManager with leader lines and label positioning

Summary:

- Added CalloutManager class for rendering golden WebGL leader lines (THREE.Line) from marker surface points radially outward by 0.25 world units.
- Supports visibility modes: always (front-facing), hover/click (start hidden, shown via showCallout/hideCallout), none (skipped).
- Backface culling via updateVisibility(cameraPosition, globeQuaternion) — applies globe quaternion to surface normals before dot product with camera direction.
- createCSS2DLabels(CSS2DObject) for browser-side CSS2D label creation with accessibility attributes (role, aria-label, tabindex).
- Label text resolved from calloutLabel[locale] falling back to name[locale].
- Added 6 unit tests in tests/callout-manager.test.js; all pass.

Diff (paths):

- src/renderer/calloutManager.js
- tests/callout-manager.test.js
- RELEASE_NOTES.md
- .GIT_NEXT_COMMIT_MESSAGE.md
