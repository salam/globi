feat: add graticuleBuilder for lat/lon grid lines

Summary:
- Added createGraticule(options) returning a THREE.LineSegments graticule mesh.
- Latitude lines every 30° from −60° to +60°, longitude lines every 30° from −150° to +180°.
- Configurable color (default 0xbed8ff) and opacity (default 0.16).
- Material: transparent, depthWrite: false, offset 0.001 above sphere surface.
- Added 4 unit tests in tests/graticule-builder.test.js; all pass.

Diff (paths):
- src/renderer/graticuleBuilder.js
- tests/graticule-builder.test.js
- .GIT_NEXT_COMMIT_MESSAGE.md
